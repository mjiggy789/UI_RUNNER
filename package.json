{
    "name": "parkour-bot-embed-service",
    "version": "1.0.0",
    "description": "Parkour bot embed service monorepo",
    "private": true,
    "workspaces": [
        "packages/*",
        "services/*"
    ],
    "scripts": {
        "build": "node build.js",
        "predev:full": "lsof -ti :3000,3001,3002 | xargs kill -9 || true",
        "dev:full": "npm run build && concurrently --kill-others \"npx serve -l 3000 release\" \"npx ts-node services/config-api/src/index.ts\" \"npx ts-node services/telemetry/src/index.ts\"",
        "test:checksum": "npx ts-node scripts/test-world-checksum.ts",
        "test": "npm run test:checksum"
    },
    "devDependencies": {
        "@types/node": "^25.2.3",
        "concurrently": "^9.2.1",
        "crypto": "^1.0.1",
        "esbuild": "^0.27.3",
        "eslint": "^8.0.0",
        "prettier": "^3.0.0",
        "serve": "^14.2.5",
        "tweetnacl": "^1.0.3",
        "typescript": "^5.0.0"
    },
    "keywords": [],
    "author": "",
    "license": "ISC"
}
